describe('to edit', () => {

  // beforeAll(async () => {
  //   const url = `file://${process.cwd()}/assets/static1/index.html`
  //   await page.goto(url);
  // });

  // it('action edit create', async () => {
  //   expect(1).toBe(1)
  //   expect(page).not.toHave({selector: 'p'})
  //   expect(page).not.toHave({selector: 'p', text: 'hello'})
  //   expect(page).to({action:'edit', editMode: 'create', create: {tagName: 'p', attrs:{id:'foo'}, innerHTML: 'hello'}})
  //   expect(page).toHave({selector: 'p'})
  //   expect(page).toHave({selector: 'p', text: 'hello'})
  // })

  it('action edit create', async () => {
    expect(1).toBe(1)
})
})
